/**
 * Candidate Scoring Service
 *
 * Evaluates candidates across multiple dimensions:
 * - Skills match
 * - Education relevance
 * - Experience alignment
 * - Application quality metrics
 * 
 * For modularity and to adhere to project's code principles (max 300-400 lines per file),
 * this service has been refactored into separate modules in the scoring directory.
 */

// Forward exports from the modular implementation
export * from './scoring';
